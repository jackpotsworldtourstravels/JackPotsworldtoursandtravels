# PostgreSQL connection string (Neon/Railway/local). Example Neon format:
# postgresql+psycopg2://user:password@ep-xxxx.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/jackpotsworldtours

# Generate a strong random value, e.g.: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=change-this-to-a-random-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
RESET_TOKEN_EXPIRE_MINUTES=60

# Comma-separated list of frontend origins allowed to call this API
CORS_ORIGINS=http://127.0.0.1:5500,http://localhost:5500,http://127.0.0.1:5501,http://localhost:5501

# Where the deployed frontend lives — used to build the absolute link in reset-password emails
FRONTEND_BASE_URL=https://YOUR_DOMAIN

# SMTP for password-reset emails. Leave SMTP_HOST unset to skip sending (a warning
# is logged instead) — useful for local dev without real email credentials.
# Gmail example: host=smtp.gmail.com, port=587, username=you@gmail.com,
# password=an App Password (not your normal Gmail password).
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=true
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=JackPots World Tours & Travels

# Mark this host as dev/test: login OTPs are logged and returned in the API
# response instead of being emailed. Everything else SMTP does — the contact
# form, password-reset links — is unaffected.
# LOCAL AND CI ONLY — never set this on a deployed host: it hands anyone who
# knows a password the second factor too. Off by default. It exists because
# tests/config.py signs in by reading `dev_otp` from the login response, and
# configuring SMTP (which the contact form needs) would otherwise stop the
# whole suite from being able to log in.
OTP_DEV_ECHO=false

# ---------------------------------------------------------------------------
# Taking money from a customer (B2C checkout). Off by default.
# ---------------------------------------------------------------------------
# PAYMENT_PROVIDER: none | razorpay | hdfc | mock
#   none      no online payment offered; the checkout shows its honest
#             "no payment is taken yet" notice (the current behaviour)
#   razorpay  Razorpay Payment Gateway - INR, UPI Intent (mobile) / UPI QR
#   hdfc      HDFC SmartGateway - hosted payment page; see HDFC_SG_* below
#   mock      takes no money and can never report a success. Local and CI only;
#             refused unless PAYMENT_ENVIRONMENT is test/sandbox/local.
PAYMENT_PROVIDER=none

# test | live. A live host logs a warning on every boot that it takes real
# money. This does NOT pick the credentials - the keys below do that.
PAYMENT_ENVIRONMENT=test

# Seconds the provider gets to answer before an attempt is abandoned.
PAYMENT_TIMEOUT_SECONDS=20

# Razorpay credentials. Required when PAYMENT_PROVIDER=razorpay.
#
# ONLY RAZORPAY_KEY_ID MAY EVER REACH A BROWSER - it is the publishable key
# Razorpay's own checkout script needs. RAZORPAY_KEY_SECRET signs API calls and
# RAZORPAY_WEBHOOK_SECRET verifies incoming deliveries; neither is ever sent to
# a page, returned by an endpoint, or written to a log.
#
# Test-mode keys start rzp_test_; live keys start rzp_live_. The webhook secret
# is set by you in the Razorpay Dashboard when you create the webhook, and must
# match this value exactly or every delivery will be rejected.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=

# RUNNING RAZORPAY LOCALLY WITHOUT BREAKING THE TEST SUITE
# tests/verify_payments.py drives the gateway through PAYMENT_PROVIDER=mock: it
# signs webhooks with the mock secret and expects a provider it can stub. Setting
# the three keys above and PAYMENT_PROVIDER=razorpay in THIS file reconfigures
# that suite too, which then posts invalid signatures at the real adapter and
# opens real test-mode orders on the account.
#
# So keep this file on mock and put the Razorpay settings in `.env.razorpay`
# (gitignored by `backend/.env.*`), then:
#
#     python run_razorpay.py
#
# It exports them into the process environment -- which pydantic ranks above
# this file -- so they apply to that one server and nothing else. It also
# refuses any key that does not start with rzp_test_.
#
# A LAPTOP CANNOT RECEIVE A WEBHOOK. Razorpay cannot deliver to 127.0.0.1, so
# nothing would ever confirm a booking on its own. That is what
# POST /api/customer/package-bookings/{ref}/reconcile is for: it runs the same
# verify_and_capture the webhook runs, on demand, and the payment screen calls
# it while polling. Point a public tunnel at /api/webhooks/payments/razorpay
# when you want to exercise the webhook path itself.

# ---------------------------------------------------------------------------
# HDFC SmartGateway (API integration, Basic Auth) - PAYMENT_PROVIDER=hdfc
# Setup, flow and troubleshooting: docs/HDFC_SMARTGATEWAY.md
#
# NEVER COMMIT REAL VALUES. Only HDFC_SG_MERCHANT_ID may reach a browser (HDFC
# sends it to the frontend itself). The API key, webhook password and response
# key are server-side only and are never logged or returned by any endpoint.
#
# UAT ONLY FOR NOW. There is no default base URL. UAT per HDFC's docs:
#   HDFC_SG_BASE_URL=https://smartgateway.hdfcuat.bank.in
# The adapter REFUSES HDFC's production host while PAYMENT_ENVIRONMENT=test,
# and refuses the UAT host while PAYMENT_ENVIRONMENT=live.
# ---------------------------------------------------------------------------
HDFC_SG_BASE_URL=
# Issued by the bank.
HDFC_SG_MERCHANT_ID=
# Dashboard -> Payments -> Settings -> Security -> API Keys -> Create New API Key.
HDFC_SG_API_KEY=
# payment_page_client_id. Leave empty: "hdfcmaster" on the UAT host and the
# merchant id elsewhere, which is what HDFC's docs prescribe.
HDFC_SG_CLIENT_ID=
# x-resellerid. HDFC documents the value hdfc_reseller.
HDFC_SG_RESELLER_ID=hdfc_reseller
# Where HDFC sends the customer back after paying. https, a domain (not an IP),
# and NO query string - HDFC's rules. Leave empty for
# {FRONTEND_BASE_URL}/api/payments/hdfc/return.
HDFC_SG_RETURN_URL=
# Dashboard -> Payments -> Settings -> Webhook tab. Set the Webhook URL to
# https://YOUR_DOMAIN/api/webhooks/payments/hdfc and choose a username and
# password there; put the same two values here. HDFC sends them as
# Authorization: Basic base64(username:password). No special characters in
# the username (HDFC's rule).
HDFC_SG_WEBHOOK_USERNAME=
HDFC_SG_WEBHOOK_PASSWORD=
# Optional. The dashboard "Response Key", with Settings -> General ->
# "Use signed response" = Yes. When set, a return without a valid HMAC-SHA256
# signature is not believed. Either way the payment is verified server-side
# with the Order Status API before anything is confirmed.
HDFC_SG_RESPONSE_KEY=
HDFC_SG_TIMEOUT_SECONDS=20

# ---------------------------------------------------------------------------
# TrustBrick partner payments
#
# TrustBrick holds the Razorpay merchant account; this platform does not. With
# these set, a booking can be paid for by asking TrustBrick to open an order on
# its account. NONE OF THESE IS A RAZORPAY CREDENTIAL - the key secret and the
# Razorpay webhook secret stay on TrustBrick and never come here.
#
# LEAVING THESE UNSET CHANGES NOTHING. Payments continue to use
# PAYMENT_PROVIDER exactly as they do today.
# ---------------------------------------------------------------------------
# TrustBrick's origin. No trailing slash, no path.
TRUSTBRICK_BASE_URL=
# The partner identity TrustBrick issued. Not a secret: it travels in a header
# so TrustBrick knows who is calling.
TRUSTBRICK_KEY_ID=
# Signs OUR requests TO TrustBrick. Must equal TrustBrick's
# PARTNER_<SLUG>_SECRET exactly, or every request is refused as a forgery.
TRUSTBRICK_SECRET=
# Verifies TrustBrick's callbacks TO US. Must equal TrustBrick's
# PARTNER_<SLUG>_CALLBACK_SECRET exactly. Use a DIFFERENT value from the one
# above, so a signature we produce cannot be verified by the key that
# authorises us.
TRUSTBRICK_CALLBACK_SECRET=
# How long TrustBrick gets to answer. A timeout is reported distinctly from a
# refusal: after one we do not know whether an order exists, and the retry must
# reuse the same idempotency key rather than opening a second.
TRUSTBRICK_TIMEOUT_SECONDS=20
# Optional. Razorpay's PUBLISHABLE key, as TrustBrick reports it. The adapter
# learns this from every create response; setting it makes a rebuilt checkout
# deterministic for a customer who reloads after a restart. Never a secret.
TRUSTBRICK_PUBLISHABLE_KEY=
#
# WHICH BOOKINGS GO THROUGH TRUSTBRICK. Comma-separated booking references.
#
# THIS IS THE ONLY THING THAT ROUTES A PAYMENT TO TRUSTBRICK, AND IT FAILS
# CLOSED. Empty - the default - means every booking uses PAYMENT_PROVIDER as it
# does today. A reference named here, and only such a reference, goes through
# TrustBrick instead. That is how the path is exercised against one real
# booking without moving the live Razorpay flow; reverting is deleting a string.
#
#   TRUSTBRICK_PILOT_BOOKING_REFS=JPP000123
TRUSTBRICK_PILOT_BOOKING_REFS=
#
# TrustBrick calls back to POST /api/webhooks/payments/trustbrick - the same
# route every provider uses. Set TrustBrick's PARTNER_<SLUG>_CALLBACK_URL to
# exactly that path: the signature covers the request target, so a different
# path fails verification rather than 404ing.

# The deferred-event sweep. A money-moving webhook that could not be verified
# when it arrived (provider unreachable, payment still in flight) stays
# deferred; this is what comes back for it. Turning it off means those events
# are never retried and a customer who paid is never confirmed.
PAYMENT_SWEEP_ENABLED=true
PAYMENT_SWEEP_INTERVAL_MINUTES=5
PAYMENT_SWEEP_BATCH_SIZE=100

# Where booking documents (passport/visa scans) are written. Never inside a
# served directory. In the Docker image this defaults to /app/uploads — on a
# host with no persistent disk that directory is wiped on every restart, so
# point it at a mounted volume before taking real customer uploads.
# UPLOAD_ROOT=/app/uploads
# MAX_UPLOAD_MB=10

# Storage backend for those documents: "local" (UPLOAD_ROOT above) or "s3".
# Use s3 on any host where the server is disposable — an EC2 instance refresh
# takes its disk with it, and these are identity documents.
#
# The bucket MUST be private (Block Public Access on). Downloads are proxied
# through the authenticated endpoint in both modes; nothing here ever issues a
# presigned URL. Credentials come from the instance's IAM role — do not put
# access keys in this file.
# STORAGE_BACKEND=s3
# S3_BUCKET=jackpotsworld-documents
# S3_REGION=ap-south-1
# S3_PREFIX=documents
# S3_SSE=AES256
# UPLOAD_ROOT is still used in s3 mode, as the scratch directory an upload is
# streamed to before it is sent on. It needs room for one file, not for all.

# Optional: control the admin account created by the 0003_seed_admin migration.
# If ADMIN_SEED_PASSWORD is not set, a random password is generated and printed
# once to the migration output — change it after your first login either way.
# ADMIN_SEED_EMAIL=admin@jackpotsworldtours.com
# ADMIN_SEED_PASSWORD=

# --- Voice calling (CR-10) --------------------------------------------------
# A kill switch: false hides the call buttons and leaves chat untouched.
VOICE_CALLS_ENABLED=true
STUN_URLS=stun:stun.l.google.com:19302,stun:stun1.l.google.com:19302
# TURN IS NOT OPTIONAL IN PRODUCTION. STUN alone cannot connect peers behind
# symmetric or carrier-grade NAT, which is standard on Indian mobile networks —
# those calls do not degrade, they never connect. See docs/CR-10_VOICE_CALLS.md
# for the coturn setup. Left empty, the app still runs and logs a warning every
# time a browser asks for an ICE config.
TURN_URLS=
TURN_USERNAME=
TURN_PASSWORD=

# --- Hotelbeds / HBX Group (hotel CONTENT catalogue) ------------------------
# BOTH VALUES ARE SECRET. Unlike Razorpay there is no publishable half: the key
# identifies the account and the secret signs every request, so neither may
# ever be sent to a browser, written into a page, or logged.
#
# Obtain them by registering at https://developer.hotelbeds.com — the sandbox
# is free and self-service, and issues the key and secret immediately.
#
# LEAVING THESE EMPTY IS A SUPPORTED STATE. The catalogue sync refuses to run
# and explains why; the site keeps serving whatever hotels are already in the
# database. Nothing else in the application depends on them.
HOTELBEDS_API_KEY=
HOTELBEDS_SECRET=

# Sandbox by default. Production is a different host, costs money per call and
# needs a signed contract — pointing at it should be a deliberate edit.
# MIND THE SANDBOX QUOTA: 50 requests PER DAY, then HTTP 403 until it resets.
HOTELBEDS_CONTENT_BASE_URL=https://api.test.hotelbeds.com/hotel-content-api/1.0
HOTELBEDS_BOOKING_BASE_URL=https://api.test.hotelbeds.com/hotel-api/1.0
HOTELBEDS_TIMEOUT_SECONDS=60
# Rows per request. Higher means fewer requests, which is what the daily quota
# actually constrains.
HOTELBEDS_PAGE_SIZE=1000
HOTELBEDS_COUNTRY_CODE=IN
HOTELBEDS_LANGUAGE=ENG

# --- Travel Assistant (the customer-facing panel and voice popup) -----------
# LEAVING THIS AT none IS THE SUPPORTED STATE, and not a degraded one: the
# built-in reader understands every rule the routing brief lists, needs no
# vendor, no key and no network call, and is what the verification script
# asserts. A provider only widens tolerance for unusual phrasing.
#
#   none     built-in reader (default)
#   openai   an OpenAI-compatible endpoint. Needs TRAVEL_AI_API_KEY.
#   local    the same protocol on a model you host (Ollama, vLLM, LM Studio).
#            Needs TRAVEL_AI_BASE_URL; no key.
#
# WHAT A PROVIDER IS TRUSTED WITH: one intent name and the place names it
# copies out of the traveller's own sentence, every one of which is checked
# back against that sentence before it is used. It never writes the reply and
# is never shown a fare, a room, a booking or a customer — so it cannot state
# a price wrongly, because it is never told one.
TRAVEL_AI_PROVIDER=none
TRAVEL_AI_API_KEY=
TRAVEL_AI_BASE_URL=https://api.openai.com/v1
TRAVEL_AI_MODEL=gpt-4o-mini
# Short on purpose: this sits in front of somebody speaking into a microphone,
# and the rules answering at once beats a spinner.
TRAVEL_AI_TIMEOUT_SECONDS=6
