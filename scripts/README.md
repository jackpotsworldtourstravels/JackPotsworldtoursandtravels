# `scripts/`

Reserved for repo-committed developer/ops scripts (data backfills,
one-off migrations helpers, deploy helpers beyond what's in `deploy/`).

Empty for now — no such scripts exist in this repo yet. Ad hoc
introspection/analysis scripts used during development live outside the
repo (in the assistant's scratch directory) and were never meant to be
committed; nothing was moved out of here to create this file.
